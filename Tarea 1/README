# Tarea 1: Salarios en el mundo de la Ciencia de Datos

## Integrantes
- José Pinto
- Vicente Ruiz

## Instrucciones de uso

Notar que todas las instrucciones van destinadas a un ambiente unix, probadas
especificamente en wsl2 con Ubuntu 22.04.3 LTS, en caso de correr en otros ambientes
puede consider python en vez de python3 en los comandos de terminal.
### Requisitos
- Python 3.7 o superior
- pip
- Archivo de datos "Salaries.csv" en la carpeta "Tarea 1"

### Instrucciones

Antes de correr index.py cree un ambiente virtual con venv e intale el requirements.txt:

Comandos útiles:
- Crear ambiente virtual venv
python<version> -m venv <virtual-environment-name>
python3 -m venv env

- Instalat virtualev (venv)
pip install virtualenv

- Activar ambiente virtual de nombre env.
source env/bin/activate

- Desactivar ambiente virutal
deactivate

- Una vez activado el ambiente, instalar el requirements.txt
pip install -r requirements.txt

Para correr el programa, ejecute el siguiente comando:
```bash
python3 index.py
```

El programa se ejecutará en el puerto 8050, por lo que puede acceder a él a través de la siguiente URL:
```bash
http://localhost:8050/
```
